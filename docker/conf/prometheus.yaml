global:
  scrape_interval: 1m
  scrape_timeout: 1m

scrape_configs:
  - job_name: 'collector'
    scrape_interval: 1m
    scrape_timeout: 1m
    static_configs:
      - targets: ['collector:9464']

    tls_config:
      insecure_skip_verify: true